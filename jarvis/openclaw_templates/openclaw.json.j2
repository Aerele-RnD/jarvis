{
  "gateway": {
    "mode": "local",
    "port": 18789,
    "bind": "lan",
    "auth": {
      "mode": "token",
      "token": "{{ gateway_token }}"
    },
    "http": {
      "endpoints": {
        "chatCompletions": { "enabled": true },
        "responses": { "enabled": true }
      }
    }
  },
  "agents": {
    "defaults": {
      "model": { "primary": "{{ provider_id }}/{{ model }}" }
    }
  },
  "models": {
    "providers": {
      "{{ provider_id }}": {
        "agentRuntime": { "id": "pi" },
        "baseUrl": "{{ base_url }}",
        "apiKey": {
          "source": "file",
          "provider": "llm_key",
          "id": "value"
        },
        "models": [
          { "id": "{{ model }}", "name": "{{ model }}" }
        ]
      }
    }
  },
  "mcp": {
    "servers": {
      "jarvis": {
        "url": "http://host.docker.internal:8000/api/method/jarvis.mcp.serve",
        "transport": "streamable-http",
        "headers": {
          "Authorization": "Bearer {{ gateway_token }}"
        }
      }
    }
  },
  "plugins": {
    "entries": {
      "jarvis-openclaw-plugin": {
        "hooks": { "allowConversationAccess": true }
      }
    }
  },
  "channels": {},
  "secrets": {
    "providers": {
      "llm_key": {
        "source": "file",
        "path": "/home/node/.openclaw/llm.key",
        "mode": "singleValue"
      }
    }
  }
}
