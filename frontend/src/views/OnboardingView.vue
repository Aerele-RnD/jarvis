<template>
	<div />
</template>

<script setup>
// Stub for the /onboarding route (Phase 2, Task 1). The wizard UI (mode
// select, managed signup/pay, self-host connect) lands in a later task —
// this placeholder exists so the lazy route import resolves and the SPA
// build passes.
</script>
